#!/bin/bash

source common

# Nvidia

sudo_run_cmd "apt-get install -y \
    mesa-utils \
    nvidia-settings \
    nvidia-367"

sudo_run_cmd "apt-get install -y \
    bumblebee \
    bumblebee-nvidia \
    primus \
    linux-headers-generic"
